---
import Layout from '../layouts/Layout.astro'
import ReceiveSection from '../components/ReceiveSection'

// Get the notification ID from URL query params
const url = new URL(Astro.request.url)
const requestId = url.searchParams.get('id')
---

<Layout title="收到挪车请求">
  <main class="bg-calm-bg text-calm-text h-dvh overflow-hidden flex flex-col font-calm p-4">
    <div class="container mx-auto flex-1 flex flex-col justify-center min-h-0">
      <div class="bg-white rounded-calm-container shadow-sm p-6 short:p-4 max-w-lg w-full text-center mx-auto flex flex-col min-h-0">
        <div class="inline-block bg-calm-primary text-white rounded-full px-4 py-1 mb-4 short:mb-2 text-xs font-medium tracking-wide self-center shrink-0">📬 收到通知</div>

        <h1 class="text-3xl short:text-2xl font-light text-slate-800 mb-4 short:mb-2 tracking-wide shrink-0">不着急。</h1>

        <ReceiveSection client:only="react" />

        <div class="mt-8 text-xs text-calm-text opacity-40">
          请求 ID: <span>{requestId}</span>
        </div>
      </div>
    </div>
  </main>
</Layout>
